VITE_FORMSPREE_ID=your_formspree_form_id

# Canonical production URL — used in index.html meta tags (og:url, og:image, canonical),
# JSON-LD structured data, and to generate public/sitemap.xml + public/robots.txt
# (see scripts/generate-seo.mjs, runs automatically via the "prebuild" script).
# No trailing slash.
VITE_SITE_URL=https://roobiksbuild.vercel.app
